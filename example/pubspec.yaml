name: example
description: Example flutter project for flutter lazy listview

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  faker: ^2.0.0
  avatars: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lazy_listview:
    path: ../
flutter:
  uses-material-design: true
  assets:
    - assets/